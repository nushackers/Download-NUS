/** @jsx React.DOM */
(function(Dan){
    var loginForm = Dan.loginForm = React.createClass({
        render: function() {
            return (
                <div>login</div>
            );
        }
    });
})(window.Dan ? window.Dan : window.Dan = {});